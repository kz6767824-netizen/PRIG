# Bonus Example: Breaking — Same Case, with LLM Explanations (Groq)

**Input SQL:**
```sql
ALTER TABLE orders DROP COLUMN shipping_address;
```

**Overall Severity: 🚨 Breaking**

*✨ Explanations below generated by LLM (Groq) where available, with automatic fallback to the template.*

---

## PR Impact Report: `orders`

### Column: `shipping_address`

- **Table:** `orders`
- **Column:** `shipping_address`
- **Severity:** `Breaking`

**Suggestion (LLM-generated):** Dropping the 'shipping_address' column from the orders table poses a breaking risk, as it will directly impact the daily_revenue_dashboard dataset. This change may cause errors or data inconsistencies in the dashboard's calculations or visualizations. To mitigate this risk, it is recommended to update the daily_revenue_dashboard dataset to remove any references to the 'shipping_address' column before proceeding with the drop operation. The data engineering team should review and modify the affected dataset to ensure compatibility with the updated orders table. By doing so, the team can ensure a smooth transition and prevent potential data disruptions.

---

**Note:** These suggestions reflect general best-practice patterns and do not account for your team's specific constraints, release cycle, compliance deadlines, or urgency. A human reviewer should confirm this approach fits the actual situation before merging.

Severity classification is fully deterministic and reproducible. If an LLM-generated explanation is used instead of the template, its exact wording may vary slightly between runs on the same input — the severity label itself will not.

**PII detection:** this version does not attempt automatic column-level PII detection. Live testing confirmed the tag WRITE path works (`add_tags` with `column_paths`), but no read path currently available through the Agent Context Kit tools surfaces column-level tags back. See README for detail.

---

*Write-back: Dry run mode was ON for this example — nothing was written to DataHub.*

*Note: this LLM response is non-deterministic — re-running the exact same input may produce differently-worded (but factually consistent) output. The severity label (Breaking) is always deterministic regardless of explanation mode.*
